import ChildTimeInTimeOut from "./components/child-timein-timeout";

const TimeinTimeout = async () => {

    return (
        <>
            <ChildTimeInTimeOut />
        </>
    );
};

export default TimeinTimeout;
